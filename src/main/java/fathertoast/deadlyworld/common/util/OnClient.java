package fathertoast.deadlyworld.common.util;

/**
 * This annotation's only purpose it to act
 * as a marker for readability. Unlike Forge's
 * {@link net.minecraftforge.api.distmarker.OnlyIn},
 * this annotation does not actually strip away any
 * code from any logical side.
 */
public @interface OnClient {}